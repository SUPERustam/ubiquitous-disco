#include <bits/stdc++.h>

#define pb push_back
#define in insert
#define sz(x) (long long)x.size()
#define all(x) x.begin(), x.end()
#define ll long long

using namespace std;

ll ans, n, m, h, k, p, q, t, a, b, c, d, x, y, z;
signed main() {
  ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  vector<ll> a, b;
  cin >> n >> m;
  for (ll i = 0; i < n; ++i) {
    cin >> c;
    a.pb(c);
  }
  for (ll i = 0; i < m; ++i) {
    cin >> c;
    b.pb(c);
  }

  return 0;
}
